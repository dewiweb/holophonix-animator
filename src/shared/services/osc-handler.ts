import { OSCTrackHandler } from './osc-track-handler';

/**
 * Main OSC Handler Service
 * Exports the track handler as the main handler for backward compatibility
 */
export class OSCHandler extends OSCTrackHandler {}